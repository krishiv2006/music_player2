# No API keys required!
# The Sonic Immersive uses the iTunes Search API (free, no auth)
# and the Apple Music RSS Feed (free, no auth).

# Optional: Port for the API server (default: 3001)
PORT=3001

# Optional: App URL for production deployments
APP_URL=http://localhost:3000
