# Frontend Environment Variables
# Set this to your Render backend URL (no trailing slash)
# Local development uses Vite proxy, so this is only needed in production
VITE_API_BASE_URL=https://music-player2-x1li.onrender.com